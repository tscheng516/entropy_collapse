# ViT5 experiments — ViT-5 (Wang et al., 2026, arXiv:2602.08071) integration
# Tested with Python 3.10  (mirrors apple/ml-sigma-reparam vision/environment.yaml)
# Reference: https://github.com/apple/ml-sigma-reparam/blob/main/vision/environment.yaml
#
# Quick install (CPU / MPS):
#   pip install -r ViT5/requirements.txt
#
# CUDA 11.8:
#   pip install --index-url https://download.pytorch.org/whl/cu118 \
#       torch==2.6.0 torchvision==0.21.0
#   pip install -r ViT5/requirements.txt
#
# CUDA 12.1:
#   pip install --index-url https://download.pytorch.org/whl/cu121 \
#       torch==2.6.0 torchvision==0.21.0
#   pip install -r ViT5/requirements.txt

# Core — pinned to the same commit used by ViT/ for reproducibility
torch==2.6.0
torchvision==0.21.0
git+https://github.com/rwightman/pytorch-image-models@9ee846ff0cbbc05a99b45140aa6d84083bcf6488

# ViT-5 specific — 2-D rotary position embeddings (rope.py)
einops

# Science / plotting
numpy==1.26.4
scipy==1.11.4
matplotlib==3.8.2

# Logging and data
wandb==0.21.3
datasets==2.19.2
